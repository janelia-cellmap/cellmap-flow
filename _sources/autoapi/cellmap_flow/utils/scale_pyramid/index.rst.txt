cellmap_flow.utils.scale_pyramid
================================

.. py:module:: cellmap_flow.utils.scale_pyramid


Attributes
----------

.. autoapisummary::

   cellmap_flow.utils.scale_pyramid.logger
   cellmap_flow.utils.scale_pyramid.RAW_SHADER
   cellmap_flow.utils.scale_pyramid.PREDICTION_SHADER
   cellmap_flow.utils.scale_pyramid.PREDICTION_COLORS


Classes
-------

.. autoapisummary::

   cellmap_flow.utils.scale_pyramid.ScalePyramid


Functions
---------

.. autoapisummary::

   cellmap_flow.utils.scale_pyramid.prediction_shader
   cellmap_flow.utils.scale_pyramid.get_raw_layer


Module Contents
---------------

.. py:data:: logger

.. py:data:: RAW_SHADER
   :value: Multiline-String

   .. raw:: html

      <details><summary>Show Value</summary>

   .. code-block:: python

      """#uicontrol invlerp normalized(range=[{lo:.6g}, {hi:.6g}], window=[{wlo:.6g}, {whi:.6g}]);
      #uicontrol vec3 color color(default="white");
      void main(){{emitRGB(color * normalized());}}"""

   .. raw:: html

      </details>



.. py:data:: PREDICTION_SHADER
   :value: Multiline-String

   .. raw:: html

      <details><summary>Show Value</summary>

   .. code-block:: python

      """#uicontrol invlerp normalized(range=[{lo:.6g}, {hi:.6g}], window=[{wlo:.6g}, {whi:.6g}]);
      #uicontrol vec3 color color(default="{color}");
      void main(){{emitRGB(color * normalized());}}"""

   .. raw:: html

      </details>



.. py:data:: PREDICTION_COLORS
   :value: ['red', 'green', 'blue', 'yellow', 'purple', 'orange', 'cyan', 'magenta']


.. py:function:: prediction_shader(color, value_range=None)

   Shader for a model output layer, over the range the chain produces.

   Falls back to [0, 1] when the range is undetermined. That is a guess, but
   the previous default was ``range=[0.5, 0.5]`` -- lo == hi makes invlerp a
   step function rather than a ramp, so every value above 0.5 rendered as
   solid colour. After a DefaultPostprocessor (0-255) that is the entire
   prediction.


.. py:function:: get_raw_layer(dataset_path, normalize=True, wrap_raw=True)

.. py:class:: ScalePyramid(volume_layers)



   A neuroglancer layer that provides volume data on different scales.
   Mimics a LocalVolume.

   :param volume_layers: One ``LocalVolume`` per provided resolution.
   :type volume_layers: ``list`` of ``LocalVolume``


   .. py:attribute:: min_voxel_size


   .. py:attribute:: max_voxel_size


   .. py:attribute:: dims


   .. py:attribute:: volume_layers


   .. py:property:: volume_type


   .. py:property:: token


   .. py:method:: info()


   .. py:method:: get_encoded_subvolume(data_format, start, end, scale_key=None)


   .. py:method:: get_object_mesh(object_id)


   .. py:method:: invalidate()

      Mark the data invalidated.  Clients will refetch the volume.



